# このリポジトリの約束

目的と使い方は README.md を参照のこと。ここには**手を入れるときの約束**だけを書く。

姉妹リポジトリ（環境 `AnlEnvMngr` / データ `AnlDataMngr` / 投入
`KlabProcCompletionChecker` / 処理 9 本）は、同じ階層に clone してある前提で
`../<名前>/` で参照する。詳しい一覧は `AnlEnvMngr/AGENTS.md` に在る。

----

## 版はタグで刻む。ブランチは `main` だけ

**`main` 以外のブランチを作らない。**版の記録（`v20260923` のような日付の版）は
**注釈付きタグ**で残す。

```bash
git tag -a v20260923 -m "この版が何かを書く"
git push origin refs/tags/v20260923
```

ブランチにしないのは、**版の記録は動いてはいけない**からである。ブランチは
書き込める入れ物なので、**気づかないうちにそこへコミットが積まれる**
（`git status` は正常に見え、`main` だけが進まない）。タグにはコミットできないので、
その事故が起きようがない。人が 1 人で、あとは AI が触る木では、
**覚えることが少ない規則ほど守られる。**

実利もある。タグがあると `git describe --tags` が

```
v20260923-5-gabc1234      直前の版から 5 commit 目
```

を返す。40 桁の hash より、`invocation.json` の来歴として人に読める。

過去の版を直す必要が出たときは、**そのときタグから生やす**。

```bash
git switch -c fix/v20260923 v20260923
```

常に置いておくより、必要になってから作る方が安全である。
